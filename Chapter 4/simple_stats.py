# Finding the minimun, maximum and sum of a list of numbers:
digits = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0]
minimum = min(digits)
print(f"Minimum:\n{minimum}")
maximum = max(digits)
print(f"\nMaximum:\n{maximum}")
sum = sum(digits)
print(f"\nSum:\n{sum}")