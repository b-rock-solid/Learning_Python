# Use the range() function to tell Python to skip numbers in a range
# Pass a third argument to range(), Python uses this value as a step size when generating numbers
# Example: Listing even numbers between 1 and 10:
even_numbers = list(range(2, 11, 2))
print(even_numbers)