message = "One of Python's stregths is its diverse community"
print(message)
# The below message will fail due to the incorrect usage of (')
# To correct this, make it the same as above using (") rather than the (')
message = 'One of Python's streghts is its diverse comunity'
print(message)